# PROJECT TODOS

1. Change validateToken to two funcs as ATM it does two things main.go
2. Add bubbletea pages to UI ui.go
3. Get pagination to work on followed streams if there is large amount main.go
4. Get twitch chat integration to work
5. Keep selection UI open and just spawn MPV instance on the side and if user
    wants to switch the stream  function xyz checks if there is mpv instance
    running, kills it and spawn another wti the new stream
6. Get search integration to work so user can search a stream even if they dont
   follow them
7. Make README.md better
8. Make class, use case, activity, sequence diagrams etc to show I handle them
   WELL ITS JUST FOR FUN AND LEARNING EXPERIENCE
